# API functions need a hosted Mongo URI in Netlify: Site configuration → Environment variables.
# Use MONGO_URI, or the same value as MONGODB_URI / DATABASE_URL (MongoDB Atlas SRV recommended).
# Google Sign-In: set GOOGLE_CLIENT_ID (Web OAuth client ID) in Netlify env. Use a scope that includes
# BOTH Builds and Functions so Vite can bake it into the client and the API can verify Google tokens.
# Optional: VITE_GOOGLE_CLIENT_ID only if you want to override the value used at build time.

[build]
  command = "npm install && npm run build:client"
  publish = "client/dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"
  external_node_modules = [
    "express",
    "mongoose",
    "mongodb",
    "bcryptjs",
    "cookie-parser",
    "cors",
    "dotenv",
    "google-auth-library",
    "jsonwebtoken",
    "multer",
    "nodemailer",
    "serverless-http"
  ]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
